version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:16
    steps:
      - checkout

      # Simple echo for the build stage
      - run:
          name: Echo Build Stage
          command: echo "Build stage completed."

  test:
    docker:
      - image: circleci/node:16
    steps:
      - checkout

      # Simple echo for the test stage
      - run:
          name: Echo Test Stage
          command: echo "Test stage completed."

  deploy:
    docker:
      - image: circleci/node:16
    steps:
      - checkout

      # Simple echo for the deploy stage
      - run:
          name: Echo Deploy Stage
          command: echo "Deploy stage completed."

workflows:
  version: 2
  ci_cd_workflow:
    jobs:
      - build
      - test:
          requires:
            - build
      - deploy:
          requires:
            - test
